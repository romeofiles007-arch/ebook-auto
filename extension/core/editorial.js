/** Shared editorial contract. Writing, editing and page fitting must agree. */
export const EVIDENCE_RULES = `ความน่าเชื่อถือ
- ห้ามแต่งสถิติ งานวิจัย คำพูด หรือประสบการณ์ของผู้เขียน ใช้ข้อเท็จจริงจากข้อมูลที่ให้หรือแหล่งที่ตรวจสอบได้
- ตัวอย่างที่คิดขึ้นเองเพื่ออธิบาย ใช้ได้และควรใช้ แค่บอกให้รู้ว่าสมมติ ไม่เล่าให้ดูเป็นเรื่องที่เกิดขึ้นจริง`;

export const OUTLINE_RULES = `เกณฑ์บรรณาธิการก่อนส่งสารบัญ
- ระบุใน thesis ว่าผู้อ่านกลุ่มนี้จะเข้าใจหรือลงมือทำอะไรได้ และเล่มนี้ใช้วิธีอธิบายใดที่เฉพาะกับหัวข้อ
- เดินจากสิ่งที่ต้องรู้ก่อน ไปสู่การใช้จริงและการตัดสินกรณียาก แต่ละบทต้องเปลี่ยนความเข้าใจหรือความสามารถของผู้อ่าน
- แต่ละตอนมีคำถามหลักหนึ่งข้อ มีเจ้าของประเด็นชัดเจนใน claim และ not_here ถ้าสองตอนตอบได้ด้วยย่อหน้าเดียวกัน ให้รวมตอนหรือเปลี่ยนหน้าที่
- beats ต้องเป็นเนื้อหาเฉพาะที่จะอธิบาย เช่นตัวแปร กลไก การสาธิต หรือข้อผิดพลาด ไม่ใช่ป้าย “เกริ่น / อธิบาย / ตัวอย่าง / สรุป”
- takeaways ระบุสิ่งที่ผู้อ่านตรวจว่าทำได้จริง เช่นวิเคราะห์ประโยคหนึ่งประโยคหรือเลือกวิธีตามเงื่อนไข ห้ามใช้เพียง “เข้าใจความสำคัญ”
- เลือกรูปแบบตามเนื้อหา ตอนที่สอนต่อเนื่องกันใช้รูปแบบเดียวกันได้ ไม่ต้องสลับเพื่อให้ครบทุกแบบ
- อย่าสัญญาข้อมูล เคสจริง หรือผลลัพธ์ที่ยังไม่มีหลักฐานรองรับ และอย่าสร้างบทเพิ่มเพียงเพื่อเติมจำนวนหน้า`;

/**
 * หลักการเขียนร่วม — สั้นโดยตั้งใจ
 *
 * เคยยาวถึงหกพันกว่าตัวอักษร: กำหนดความยาวประโยค ห้ามคำเชื่อมบางคำ ห้ามคำกันตัว
 * บังคับว่าต้องมีของรูปธรรม บังคับว่าต้องฟันธง ห้ามประโยคคมคม ห้ามประดิษฐ์คำ
 * ทุกข้อมีเหตุผลของมัน แต่รวมกันแล้วเหลือที่ให้ตัดสินใจน้อยจนงานเขียนออกมาเกร็ง
 * — เหมือนสั่งคนเขียนว่าห้ามทำอะไรบ้างสามสิบข้อ แล้วหวังว่าจะได้งานที่อ่านสบาย
 *
 * ที่เหลือไว้มีสามอย่างที่ขาดไม่ได้จริง: เขียนให้ตรงกับใบสั่งของตอนนั้น
 * ต่อเนื่องตามลำดับสารบัญ และห้ามแต่งข้อเท็จจริง
 * เรื่องสำนวน จังหวะ ความยาวประโยค การเน้นคำ และวิธีปิดตอน ปล่อยให้เป็นการตัดสินใจของคนเขียน
 */
export function proseRules(book = {}, voice = '') {
  return `หลักการเขียนร่วมของทุกแผนก
ภาษา: ${(book.language || 'th') === 'th' ? 'ไทย' : 'อังกฤษ'} · โทน: ${book.tone || 'ชัดเจน เป็นธรรมชาติ'}
${voice}
- เขียนตามใบสั่งของตอนนี้ ตอบคำถามหลักให้จบ อยู่ในขอบเขตของตอน ไม่ล้ำเรื่องที่เป็นของตอนอื่น
- เรียงต่อตามลำดับสารบัญ รับช่วงจากสิ่งที่ผู้อ่านเพิ่งอ่านจบ และไม่สอนซ้ำสิ่งที่อธิบายไปแล้ว
- เขียนให้คนอ่านรู้เรื่องและอยากอ่านต่อ เลือกจังหวะ ความยาวประโยค ตัวอย่าง และวิธีเล่าเองตามที่เนื้อหาเรียกร้อง
- เน้นคำ ใส่หัวข้อย่อย ใช้รายการ หรือปิดตอนด้วยประโยคที่คม ใช้ได้ตามที่เห็นว่าเข้ากับตอนนั้น
- ความยาวเป็นงบสำหรับเนื้อหา ครบประเด็นแล้วจบได้ แล้วบอกเหตุผลสั้น ๆ ใน META.short_reason
- ข้อความถึงบรรณาธิการ แผนภาพ และข้อมูล META ต้องอยู่นอกเนื้อหาที่พิมพ์จริง
${EVIDENCE_RULES}`;
}

export function proseBatch({ book, chapter, sections, prevSummaries = [], prevTail, nextSection, context, output, voice }) {
  const current = new Set(sections.map((s) => s.id));
  const map = (chapter.sections || sections).map((s) =>
    `${current.has(s.id) ? 'เขียนรอบนี้' : 'บริบทเท่านั้น'} ${s.id} ${s.title} — ${s.claim || s.problem || (s.takeaways || []).join('; ')}`,
  ).join('\n');
  return `หน้าที่รอบนี้: เขียนเนื้อหาตามสารบัญที่อนุมัติแล้ว โดยรักษาขอบเขตของแต่ละตอน
${context}

แผนของบท — ใช้เพื่อกันเนื้อหาซ้ำ ห้ามเขียนตอนที่ไม่ได้สั่ง
${map}

${sections.map((s) => `ตอน ${s.id}: ${s.title}
คำถาม/ปัญหาหลัก: ${s.problem || s.title}
ข้อเสนอที่ต้องอธิบายพร้อมเหตุผล: ${s.claim || (s.takeaways || []).join('; ')}
ผลที่ต้องได้เมื่ออ่านจบ: ${s.verdict || (s.takeaways || []).join('; ')}
เนื้อหาที่ต้องครอบคลุมตามลำดับ: ${(s.beats || []).join(' → ')}
ขอบเขตที่อยู่ตอนอื่น: ${s.not_here || 'ยึดแผนบทข้างบน'}
ความยาวประมาณ ${s.quota || 1800} ${book.language === 'en' ? 'คำ' : 'อักษร'}${s.maxChars ? ` เพดาน ${s.maxChars}` : ''}`).join('\n\n')}

${prevSummaries.length ? `สิ่งที่อธิบายไปแล้ว — อ้างกลับสั้น ๆ ได้ ไม่สอนซ้ำ\n${prevSummaries.join('\n')}` : ''}
${prevTail ? `ท้ายต้นฉบับก่อนหน้า — รับความคิดต่อโดยไม่คัดลอก\n${String(prevTail).slice(-1200)}` : ''}
${nextSection ? `ตอนถัดไป: ${nextSection.title} — เว้นประเด็นของตอนนั้นไว้` : 'ปิดประเด็นที่ได้รับมอบหมายให้ครบ'}

${proseRules(book, voice)}

${output}`;
}

export function safeVoicePrompt(book, outline) {
  return `กำหนดแนวทางการเล่าเรื่องสำหรับหนังสือ ${outline.title || book.topic}
ผู้อ่าน: ${book.audience || 'ผู้อ่านทั่วไป'} · โทน: ${book.tone || 'ชัดเจน เป็นธรรมชาติ'}
แก่น: ${outline.thesis || book.topic}
สร้างแนวทางภาษาและมุมการอธิบาย ไม่สร้างบุคคลหรือประวัติผู้เขียน
who คือบทบาทการสื่อสาร เช่นผู้เรียบเรียงที่อธิบายด้วยตัวอย่าง ไม่ใช่อาชีพหรือความเชี่ยวชาญที่อ้างว่าเป็นข้อเท็จจริง
believes และ rejects คือหลักการเรียบเรียง เช่นอธิบายเหตุผลก่อนให้คำแนะนำ ไม่ต้องตั้งความเชื่อขัดกระแส
ห้ามแต่งอาชีพ ประสบการณ์ตรง ความผิดพลาดในชีวิต หรือความสำเร็จของผู้เขียน
ตอบ JSON ในบล็อกโค้ดเดียว ใช้ภาษา${book.language === 'en' ? 'อังกฤษ' : 'ไทย'}:
{"who":"บทบาทการสื่อสาร","why_this_book":"สิ่งที่อยากช่วยผู้อ่าน","believes":"แนวทางการอธิบาย","rejects":"วิธีเขียนที่เลี่ยง","was_wrong_about":"","still_unsure_about":"เรื่องที่ต้องหาข้อมูลก่อนเขียน","avoid_words":[]}`;
}

export function safeVoiceBlock(book = {}) {
  if ((book.authorVoice || 'auto') === 'off') return '';
  const free = String(book.authorVoiceText || '').trim();
  if (free) return `แนวทางผู้เขียนที่ผู้ใช้ให้: ${free}\nใช้เฉพาะข้อเท็จจริงที่ให้มา ห้ามเติมประวัติหรือเหตุการณ์ส่วนตัว`;
  const card = book.authorVoiceCard;
  if (!card) return '';
  // Legacy cards contain invented careers and memories. Never reuse those as biography.
  return `แนวทางสำนวน: ${card.why_this_book || ''}
คำที่ควรเลี่ยง: ${(card.avoid_words || []).join(', ')}
การ์ดอัตโนมัติเป็นแนวทางภาษาเท่านั้น ห้ามอ้างเป็นประวัติหรือประสบการณ์จริงของผู้เขียน`;
}

export function proseReview({ chapter, sections, bible = {}, book = {}, partLabel = '' }) {
  return `หน้าที่รอบนี้: บรรณาธิการตรวจเนื้อหา บท ${chapter.n} ${chapter.title} ${partLabel}
แก่นเล่ม: ${book.outline?.thesis || book.topic || ''}
ผู้อ่าน: ${book.audience || ''}
แผนของบท: ${(chapter.sections || []).map((s) => `${s.id}: ${s.claim || s.problem || s.title}`).join('\n')}
สรุปตอนอื่นสำหรับหาจุดซ้ำ (ไม่ใช่หลักฐานข้อความเต็ม):
${Object.entries(bible.sectionSummaries || {}).filter(([id]) => !sections.some((s) => s.id === id)).map(([id, sum]) => `${id}: ${sum}`).join('\n')}

ต้นฉบับเต็มที่ต้องตรวจ:
${sections.map((s) => `<<<ตอน ${s.id} ${s.title}>>>\n${s.md || s.text || ''}\n<<<จบตอน ${s.id}>>>`).join('\n\n')}

ตรวจตามลำดับผลต่อผู้อ่าน
1. ตอนนี้ตอบคำถามหลักหรือยัง มีข้ออธิบายหรือวิธีใช้ที่เฉพาะเจาะจงหรือเพียงกล่าวหลักกว้าง ๆ
2. มีย่อหน้าวนซ้ำ บทเกริ่นซ้ำ หรือสองตอนทำหน้าที่เดียวกันหรือไม่ ชี้ว่าควรเก็บสาระไว้ตอนใดและตัดตรงไหน
3. เหตุผลข้ามขั้น ตัวอย่างไม่พิสูจน์ข้ออ้าง หรือข้อสรุปเกินหลักฐานหรือไม่
4. มีประสบการณ์ สถิติ แหล่งอ้างอิงที่ไม่มีข้อมูลรองรับหรือไม่ ให้ขอหลักฐานหรือลดข้ออ้าง ห้ามคิดข้อมูลแทนผู้เขียน
5. ภาษาอ่านสะดุด ศัพท์ไม่ตรง ลำดับไม่ต่อเนื่อง หรือคำสัญญาค้างหรือไม่
อ้างข้อความจริงใน quote ทุกจุดที่ติ พร้อม fix ที่ทำได้เฉพาะจุด ห้ามใช้จำนวนคำ คำเชื่อม หรือความกล้าฟันธงเป็นเกณฑ์คุณภาพ
ถ้าพบเพียงความคล้ายจากสรุป ให้ระบุว่าต้องเทียบต้นฉบับอีกตอนก่อนยืนยัน ห้ามแต่ง quote ของตอนที่ไม่ได้รับมา
การตัดเนื้อหาซ้ำแล้วสั้นลงเป็นผลแก้ที่ยอมรับได้ ไม่ต้องเติมเนื้อหาแทนเพื่อรักษาจำนวนหน้า
section_verdicts ต้องมีครบทุกตอนที่ส่งมา ตอนที่ดีแล้วให้ ok โดยบอกว่าเพิ่มความเข้าใจอะไร ตอนที่ต้องแก้ให้ needs_fix และต้องมีประเด็นรองรับ verdict รวมใช้ ok หรือ needs_fix เท่านั้น
${EVIDENCE_RULES}

ตอบ JSON ในบล็อกโค้ดเดียว:
{"duplicates":[],"term_conflicts":[],"unpaid_promises":[],"reorder":[],"readability_issues":[{"section":"รหัสตอน","quote":"ข้อความจริง","what":"ปัญหา","fix":"แก้จุดใดอย่างไร"}],"section_verdicts":[{"section":"รหัสตอน","verdict":"ok","note":"เหตุผล"}],"chapter_summary":"สิ่งที่บทนี้เพิ่มให้ผู้อ่าน","verdict":"ok"}`;
}

export function editContext(book, section) {
  const outline = book.outline || {};
  const chapter = (outline.chapters || []).find((c) => (c.sections || []).some((s) => s.id === section.id));
  const plan = chapter?.sections?.find((s) => s.id === section.id) || section;
  return `แก่นเล่ม: ${outline.thesis || book.topic || ''}
โจทย์ของตอนนี้: ${plan.problem || plan.title || section.title}
ขอบเขต: ${plan.claim || ''} · เว้นไว้ตอนอื่น: ${plan.not_here || ''}
สิ่งที่บทนี้มีอยู่แล้ว: ${(chapter?.sections || []).filter((s) => s.id !== section.id).map((s) => `${s.id} ${s.claim || s.title}`).join('; ')}
การแก้ต้องรักษาเครื่องหมายภาพ ![...](fig:...) เดิมไว้ครบ ห้ามลบหรือเปลี่ยนชื่อไฟล์ภาพ กล่องที่เก็บไว้ต้องใช้ไวยากรณ์ :::box ให้ถูกต้อง แต่ตัดหรือรวมกล่องที่ซ้ำตามผลตรวจได้`;
}

export function proseRepair({ book, section, currentText, issues, voice, output }) {
  return `แก้ตอน ${section.id} ${section.title} ตามผลตรวจเฉพาะจุด
${editContext(book, section)}
ประเด็นที่ต้องแก้:
${issues.map((it, i) => `${i + 1}. [${it.label}] ${it.text}${it.fix ? ' — ' + it.fix : ''}`).join('\n')}
เก็บส่วนที่ไม่มีปัญหาไว้ แก้เหตุผล ลำดับ และคำอธิบายให้ตรงโจทย์
ตัดย่อหน้าซ้ำได้โดยไม่ต้องเติมแทน รักษาหลักฐาน เงื่อนไข และข้อจำกัดที่จำเป็น
ห้ามทำให้ข้ออ้างแรงขึ้นโดยตัดเงื่อนไข ห้ามเพิ่มข้อมูลหรือประสบการณ์ที่ไม่มีต้นทาง
ต้นฉบับ:
---
${currentText}
---
${book.contentMode === 'fiction' ? 'รักษา POV เสียงตัวละคร canon และลำดับเหตุการณ์ แก้ด้วยฉาก ไม่เพิ่มบทสอนหรือสรุปบทเรียน' : proseRules(book, voice)}
${output}`;
}
