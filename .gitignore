# งานเขียนของผู้ใช้ — ไม่ใช่ซอร์สโค้ด และเป็นทรัพย์สินของคนที่สร้างมัน
books/
extension/Ebook Projects/
*.project.json
*.meta.json

# ไฟล์ที่ระบบสร้างระหว่างทำงาน
tmp/
*.pdf
*.docx
*.epub

# เครื่องมือ/แคชของเครื่องที่ใช้พัฒนา
.serena/
.claude/
node_modules/
.DS_Store
Thumbs.db
desktop.ini

# ไฟล์ขยะจากการดาวน์โหลดที่ค้างอยู่ในโฟลเดอร์
# ต้อง escape วงเล็บเหลี่ยม ไม่งั้น git อ่านเป็น character class แล้วไม่ตรงกับชื่อไฟล์จริง
m\[1\])
